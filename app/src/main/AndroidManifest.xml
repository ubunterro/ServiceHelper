<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.ubunterro.servicehelper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.SettingsActivity"></activity>
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.DeviceHistoryActivity" />
        <activity android:name=".ui.OrdersActivity" />
        <activity
            android:name=".ui.EditRepairActivity"/>
        <activity android:name=".ui.WarehouseItemActivity" />
        <activity
            android:name=".ui.WarehouseActivity"
            android:label="@string/title_activity_warehouse"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.SearchActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.RepairDetailsActivity"
            android:label="@string/title_activity_repair_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.LoginActivity"
            android:label="LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>